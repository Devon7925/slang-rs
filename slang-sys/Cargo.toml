[package]
name = "slang-sys"
version = "0.1.0"
edition = "2024"
license = "MIT OR Apache-2.0"
publish = false

[dependencies]
paste = "1.0.15"
serde = { version = "1", features = ["derive"], optional = true}

[build-dependencies]
bindgen = "0.71.1"

[features]
serde = ["dep:serde"]

